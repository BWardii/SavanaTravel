import { OnboardClientWrapper } from "@/components/onboard/onboard-client-wrapper";

export const metadata = {
  title: "Plan Your Journey — Savana Travel",
  description: "Start your luxury travel enquiry in three simple steps.",
};

export default function OnboardPage() {
  return <OnboardClientWrapper />;
}
